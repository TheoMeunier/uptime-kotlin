enum ProbeProtocol {
	HTTP = 'HTTP',
	TCP = 'TCP',
	PING = 'PING',
	DNS = 'DNS',
	POSTGRESQL = 'POSTGRESQL',
	SQLSERVER = 'MICROSOFT SQL SERVER',
	MYSQL = 'MYSQL / MARIADB',
	REDIS = 'REDIS',
	SMTP = 'SMTP',
}

export default ProbeProtocol;
