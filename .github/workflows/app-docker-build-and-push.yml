name: App Docker build and push
on:
  push:
    tags:
      - 'v*.*.*'

defaults:
  run:
    working-directory: ./apps/app

jobs:
  app-build-docker:
    uses: ./.github/workflows/templates/build-push-docker.yml
    with:
      name: App
      image-name: app